# Test file
#kind: pipeline
#name: test-on-arm64-py
#platform:
#  arch: arm64
#steps:
#- name: test
#  image: python:3.8
#  commands:
#  - .travis/install.sh
#  - ./.travis/run.sh
#  environment:
#    TOXENV: py38 
#    LIBRESSL: 2.9.2

#kind: pipeline
#name: test-on-arm64
#platform:
#  arch: arm64
#steps:
#- name: test
#  image: docker:dind
#  volumes:
#  - name: dockersock
#    path: /var/run/docker.sock
#  commands:
#  - docker ps -a
#  - .travis/install.sh
#  - ./.travis/run.sh
#  environment:
#    TOXENV: py38
#    DOCKER: 'pyca/cryptography-runner-bullseye'
#volumes:
#- name: dockersock
#  host:
#    path: /var/run/docker.sock

kind: pipeline
name: default

steps:
- name: test
  image: docker:dind
  volumes:
  - name: dockersock
    path: /var/run
  commands:
  - sleep 5 # give docker enough time to start
  - docker ps -a

services:
- name: docker
  image: docker:dind
  privileged: true
  volumes:
  - name: dockersock
    path: /var/run

volumes:
- name: dockersock
  temp: {}
